# Data & artifacts
/data/
*.npz
*.pt
/results/trajectories/
/results/*.log

# Throwaway/external harness (Kaggle push scripts, pulled kernel outputs) —
# not reproducible from a fresh clone, not meant to be committed.
/scratch/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Environments
.venv/
.env

# Jupyter
.ipynb_checkpoints/

# Serena
.serena/

# Kaggle CLI scaffolding templates left in the repo root by `kaggle kernels init`
# (placeholder INSERT_*_HERE values, not real config -- the real per-run metadata
# lives in scratch/kaggle_*/kernel-metadata.json). Untracked since 2026-07-21.
/dataset-metadata.json
/kernel-metadata.json

# Regenerable plot output (figures/ proper is tracked; this stray dir is not).
/results/figures/
